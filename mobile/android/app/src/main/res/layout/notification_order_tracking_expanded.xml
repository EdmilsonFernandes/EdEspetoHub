<?xml version="1.0" encoding="utf-8"?>
<!--
  Notificacao ongoing de acompanhamento (visao expandida) — minimalista.
  Status (grande) + "Pedido #abc" (sutil) + barra de progresso. Sem nome de loja, sem logo,
  sem lista de etapas (so a barra indica o avanco).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="2dp"
    android:paddingEnd="2dp"
    android:paddingTop="6dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/notif_track_status"
        style="@android:style/TextAppearance.DeviceDefault.Notification.Title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textStyle="bold"
        android:singleLine="true"
        android:ellipsize="end"
        android:text="Pedido em andamento" />

    <TextView
        android:id="@+id/notif_track_order"
        style="@android:style/TextAppearance.DeviceDefault.Notification"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textSize="12.5sp"
        android:singleLine="true"
        android:ellipsize="end"
        android:text="" />

    <ProgressBar
        android:id="@+id/notif_track_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="7dp"
        android:layout_marginTop="10dp"
        android:max="100"
        android:progress="0"
        android:progressDrawable="@drawable/notif_progress_horizontal" />
</LinearLayout>
